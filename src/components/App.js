import { Reset } from "styled-reset";
import GlobalStyle from "./GlobalStyle";

export default function App() {

    return (
        <>
            <Reset />
            <GlobalStyle />
            <h1>Testando a aplicação react</h1>
        </>
    );
}